.clean-button-tiny.right add new menu item
h6 Menu Items

div(ng-controller="VenueController")
    input(type='search', placeholder='find a menu item', ng-model='query')
    ul.menu-list
        li.item(ng-repeat='item in data.menuitems | filter: query')
            {{item.name}}
